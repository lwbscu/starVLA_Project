# H200 AWAC 后训练运行命令

本文档给接手同学直接复制运行。只保留当前实际采用的一条主线：

```text
Qwen3.5-4B PI-State BC checkpoint
-> public CALVIN 数据只读加载
-> dataloader 即时计算 AWAC reward/done
-> 训练 critic
-> 训练 actor
-> 再做 CALVIN eval / rollout
```

核心原则：

- 不复制完整 CALVIN 数据集。
- 不修改 public parquet。
- 不使用 `assume_success_if_missing=true`。
- §4 单源 on-the-fly 主线不把 rollout 当训练源；**混训见 §4b**（public + rollout 1:1）。
- 不把 9B `steps_25000` 写成 30k。

## 0. 本流程用到的绝对路径

下面这些路径是当前 H200 服务器上的实际路径。后续命令都围绕这些路径展开。

| 名称 | 绝对路径 | 说明 |
|---|---|---|
| 项目根目录 | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project` | 所有命令都在这里执行 |
| Conda 根目录 | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3` | `starVLA_qwen35` 和 `calvin` 环境都在这里 |
| Python 环境 | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3/envs/starVLA_qwen35/bin/python` | 训练、readiness、parquet 写入都优先用它 |
| CALVIN public 数据父目录 | `/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d` | 只读使用，不写 parquet |
| CALVIN LeRobot 数据集 | `/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d/calvin_task_ABC_D` | `data_mix=calvin_abc_d_h200` 指向它 |
| PI-State BC 根目录 | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1` | 已训练好的 BC baseline |
| Qwen3.5-4B 模型 | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/playground/Pretrained_models/Qwen3.5-4B` | 4B base VLM |
| 4B BC checkpoint | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1/server1_pi_state/qwen35_4b/pi_state_qwen35_4b_30000step/checkpoints/pi_state_qwen35_4b_30000step/checkpoints/steps_30000_pytorch_model.pt` | 当前 AWAC 主线的 actor 初始化 |
| AWAC 输出目录 | `/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly` | critic/actor 都写到这里 |

## 1. 启动环境

这一步只做环境激活和基础验证。之前遇到过未激活环境导致 `accelerate: command not found`，所以先确认 Python 和 accelerate 都来自 `starVLA_qwen35`。

```bash
cd /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project

export PROJECT_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project
export CONDA_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3

export PATH="${CONDA_ROOT}/bin:$PATH"
source "${CONDA_ROOT}/etc/profile.d/conda.sh"
conda activate starVLA_qwen35

export STAR_VLA_PYTHON=/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3/envs/starVLA_qwen35/bin/python
export CALVIN_PYTHON=/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3/envs/calvin/bin/python
export ROLLOUT_PARQUET_PYTHON="${STAR_VLA_PYTHON}"

which python
python -c "import sys; print(sys.executable)"
"${STAR_VLA_PYTHON}" -m accelerate.commands.launch --help >/dev/null && echo "accelerate_module=ok"
```

通过标准：

- `which python` 指向 `.../envs/starVLA_qwen35/bin/python`
- 输出 `accelerate_module=ok`

如果 H200 里没有 `git`，不要卡在 `git pull`；训练本身不依赖 `git` 命令。

## 2. 设置固定变量并检查文件存在

这一步把后面要用的绝对路径都固定下来，并检查 4B base model 和 4B BC checkpoint 是否存在。

```bash
export PROJECT_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project

export PI_STATE_BATCH_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1

export H200_CALVIN_DATA_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d
export H200_CALVIN_DATA_NAME=calvin_task_ABC_D
export H200_CALVIN_DATA_MIX=calvin_abc_d_h200
export H200_CALVIN_DATASET_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d/calvin_task_ABC_D

export QWEN35_4B=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/playground/Pretrained_models/Qwen3.5-4B
export PI_STATE_CKPT_4B=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1/server1_pi_state/qwen35_4b/pi_state_qwen35_4b_30000step/checkpoints/pi_state_qwen35_4b_30000step/checkpoints/steps_30000_pytorch_model.pt

test -f "${QWEN35_4B}/config.json"
test -f "${PI_STATE_CKPT_4B}"
test -f "${H200_CALVIN_DATASET_ROOT}/meta/info.json"
test -f "${H200_CALVIN_DATASET_ROOT}/meta/modality.json"
test -d "${H200_CALVIN_DATASET_ROOT}/data"

echo "paths_ok"
```

实验记录口径：

- 当前主力后训练模型是 `Qwen3.5-4B`。
- 当前 BC checkpoint 是 `steps_30000_pytorch_model.pt`。
- 9B 只有 `steps_25000`，可以作为单独口径记录，不能写成 30k。

## 3. 严格 readiness 检查

这一步确认 BC、数据、state/action 维度和 on-the-fly reward/done 路线都满足 AWAC 入口要求。

注意：这里**不传** `--assume-success-if-missing`。当前数据 sampled parquet 已有真实 `success`，所以必须真实读取 success。

```bash
export OUT="/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/awac_readiness_onfly_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$OUT"

"${STAR_VLA_PYTHON}" /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/examples/calvin/scripts/inspect_awac_readiness.py \
  --batch-root /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1 \
  --data-root /inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d \
  --dataset-name calvin_task_ABC_D \
  --data-mix calvin_abc_d_h200 \
  --expected-step 30000 \
  --job-expected-step server1_pi_state/qwen35_9b=25000 \
  --allow-on-the-fly-rewards \
  --json-out "$OUT/awac_readiness_strict_success.json" \
  > "$OUT/awac_readiness_strict_success.md"

echo "inspect_status=$?"
sed -n '1,300p' "$OUT/awac_readiness_strict_success.md"
```

必须看到：

- `inspect_status=0`
- `overall: PASS`
- `success labels: PASS`
- `assume_success_if_missing: False`
- `AWAC reward preprocessing: PASS`
- `state metadata dim: state dim 8`
- `action metadata dim: action dim 7`

如果这里不是 PASS，不要开 critic。

## 4. 启动 4B critic

这一步训练 Q critic。BC actor 只用于初始化和提供 frozen actor，不训练 actor。数据从 public 路径只读加载，`reward/done` 在 dataloader 内即时计算。

```bash
cd /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project

export CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7
export num_processes=8
export WANDB_MODE=offline

export base_vlm=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/playground/Pretrained_models/Qwen3.5-4B
export bc_checkpoint=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1/server1_pi_state/qwen35_4b/pi_state_qwen35_4b_30000step/checkpoints/pi_state_qwen35_4b_30000step/checkpoints/steps_30000_pytorch_model.pt

export calvin_data_root=/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d
export data_mix=calvin_abc_d_h200
export include_state=true
export state_dim=8

export compute_rewards_on_the_fly=true
export assume_success_if_missing=false

export run_root_dir=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly
export run_id=awac_critic_pi_state_qwen35_4b_bc30k_onfly

bash /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/examples/calvin/train_files/run_calvin_awac_critic.sh
```

启动后必须看到类似日志：

```text
[AWAC] calvin_task_ABC_D: 803693/1071743 valid transitions (H=8)
[AWAC] calvin_task_ABC_D: computing reward/done on the fly (H=8, gamma=0.996, success_column=success, assume_success_if_missing=False)
AWAC critic phase: actor frozen; training Q network only.
```

如果看到 `assume_success_if_missing=True`，说明环境变量错了，立刻停掉重查。

## 4b. 混训 critic 一键命令（推荐，无需手工合并）

自动完成：**8 shard rollout 合并** → **从 public 成功 demo 随机抽与 rollout 等量的 expert episode** → **1:1 混训 critic**（BC `steps_30000`，训 30k，每 10k 存盘）。

在 H200 上只需执行（先完成 §1 环境激活）：

```bash
bash /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/examples/calvin/train_files/h200_awac_critic_mixed_oneclick.sh
```

脚本默认路径（可用环境变量覆盖，见下表）：

| 变量 | 默认值 |
|---|---|
| `ROLLOUT_SRC` | `.../results/rollout/pi_state_4b_awac_parallel_500seq_v1` |
| `ROLLOUT_EVAL_ROOT` | `.../awac_datasets/rollout_4b_500seq_merged/eval` |
| `CALVIN_DATA_ROOT` | `.../public/.../calvin_abc_d` |
| `bc_checkpoint` | `.../steps_30000_pytorch_model.pt` |
| `run_id` | `awac_critic_mixed_oneclick_bc30k_30k` |

输出 checkpoint：

```text
.../logs/20260520_awac_pi_state_mixed/awac_critic_mixed_oneclick_bc30k_30k/checkpoints/steps_{10000,20000,30000}_critic.pt
```

强制重新合并 rollout：`MERGE_FORCE=true bash .../h200_awac_critic_mixed_oneclick.sh`

---

### 4b.1 手工合并 8 shard rollout（仅调试；一键脚本已包含）

若尚未合并，在 H200 上执行（路径按实际 shard 调整）：

```bash
export PROJECT_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project
export ROLLOUT_SRC="${PROJECT_ROOT}/results/rollout/pi_state_4b_awac_parallel_500seq_v1"
export ROLLOUT_DST="${PROJECT_ROOT}/awac_datasets/rollout_4b_500seq_merged/eval"

mkdir -p "${ROLLOUT_DST}/rollout_lerobot"
# 将各 shard 的 rollout_lerobot 下 data/meta/videos 合并到 ROLLOUT_DST/rollout_lerobot
# 若已有合并脚本则优先用脚本；否则按 LeRobot episode 文件名去重后 rsync/cp。
for shard in "${ROLLOUT_SRC}"/qwen35_4b/shard_*/server1_pi_state/qwen35_4b/steps_30000/eval/rollout_lerobot; do
  rsync -a "${shard}/" "${ROLLOUT_DST}/rollout_lerobot/"
done

test -f "${ROLLOUT_DST}/rollout_lerobot/meta/info.json"
echo "rollout_merge_ok"
```

未合并时也可临时指定单个 shard（样本较少，仅 smoke）：

```bash
export rollout_eval_root="${PROJECT_ROOT}/results/rollout/pi_state_4b_awac_parallel_500seq_v1/qwen35_4b/shard_0/server1_pi_state/qwen35_4b/steps_30000/eval"
```

rollout 侧需已有 `success` 列；reward/done 可与 expert 一样在 dataloader **on-the-fly** 计算（不修改 public parquet）。

### 4b.2 一键启动混训（推荐）

```bash
cd /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project

export PROJECT_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project
export CONDA_ROOT=/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3
export PATH="${CONDA_ROOT}/bin:$PATH"
source "${CONDA_ROOT}/etc/profile.d/conda.sh"
conda activate starVLA_qwen35
export STAR_VLA_PYTHON=/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3/envs/starVLA_qwen35/bin/python

bash examples/calvin/train_files/run_calvin_awac_critic_mixed_h200.sh
```

默认输出目录：

```text
/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_mixed/awac_critic_mixed_bc30k_30k_bs32_save10k
```

### 4b.3 等价手写 export（需自定义 run_id 时用）

```bash
cd /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project

export CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7
export num_processes=8
export WANDB_MODE=offline
export STAR_VLA_PYTHON=/inspire/qb-ilm2/project/26summer-camp-10/26220216/miniconda3/envs/starVLA_qwen35/bin/python

export base_vlm=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/playground/Pretrained_models/Qwen3.5-4B
export bc_checkpoint=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1/server1_pi_state/qwen35_4b/pi_state_qwen35_4b_30000step/checkpoints/pi_state_qwen35_4b_30000step/checkpoints/steps_30000_pytorch_model.pt

export calvin_data_root=/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d
export rollout_eval_root=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/awac_datasets/rollout_4b_500seq_merged/eval
export data_mix=calvin_awac_mixed_h200
export balance_datasets=true

export include_state=true
export state_dim=8
export compute_rewards_on_the_fly=true
export assume_success_if_missing=false

export critic_max_train_steps=30000
export save_interval=10000
export per_device_batch_size=32

export run_root_dir=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_mixed
export run_id=awac_critic_mixed_bc30k_30k_bs32_save10k

bash examples/calvin/train_files/run_calvin_awac_critic.sh
```

启动后应看到类似日志：

```text
[AWAC] mixture: calvin_task_ABC_D len=... nominal_weight=1.0 ...
[AWAC] mixture: rollout_lerobot len=... nominal_weight=1.0 ...
[AWAC] mixture sampling: balance_by_dataset=True
AWAC critic phase: actor frozen; training Q network only.
```

### 4b.4 混训 checkpoint 验收

```bash
export CRITIC_RUN=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_mixed/awac_critic_mixed_bc30k_30k_bs32_save10k

find "${CRITIC_RUN}/checkpoints" -maxdepth 1 -type f -name 'steps_*_critic.pt' -print | sort
# 期望: steps_10000_critic.pt  steps_20000_critic.pt  steps_30000_critic.pt
```

## 5. 监控 critic

critic 输出目录是：

```text
/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly/awac_critic_pi_state_qwen35_4b_bc30k_onfly
```

另开终端时先重新激活环境，然后执行：

```bash
export CRITIC_RUN=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly/awac_critic_pi_state_qwen35_4b_bc30k_onfly

find "${CRITIC_RUN}" -maxdepth 3 -type f | sort | grep -E 'log$|txt$|json$' || true
find "${CRITIC_RUN}/checkpoints" -maxdepth 1 -type f -name 'steps_*_critic.pt' -print | sort || true
find "${CRITIC_RUN}/tensorboard" -type f | head || true
```

如果训练还在当前终端，直接看进度条中的：

- `critic_loss`
- `q_mean`
- `target_mean`

健康标准：

- 第一个 checkpoint 应出现：`checkpoints/steps_5000_critic.pt`
- 最终目标 checkpoint：`checkpoints/steps_50000_critic.pt`
- `critic_loss/q_mean/target_mean` 都是有限数，不是 `nan/inf`
- TensorBoard 目录下有 event 文件

重要提醒：训练启动时 dataloader 可能重建 public 数据目录下的 `meta/steps_data_index.pkl` cache。它不是 parquet 内容，也不是 `success/reward/done` 标签。如果后续要求 public 目录完全零写入，需要另起任务把 cache 路径迁到个人目录。

## 6. Critic 中断后的处理

当前 `train_awac_critic.py` 没有严格 resume 参数。不要把重启训练说成“从旧 checkpoint 继续训练”。

先看进程是否还在：

```bash
ps -ef | grep -E 'train_awac_critic.py|accelerate.commands.launch' | grep -v grep || true
```

再看已经保存了哪些 critic checkpoint：

```bash
export CRITIC_RUN=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly/awac_critic_pi_state_qwen35_4b_bc30k_onfly
find "${CRITIC_RUN}/checkpoints" -maxdepth 1 -type f -name 'steps_*_critic.pt' -print | sort || true
```

处理原则：

- 如果进程还在，继续等。
- 如果进程已死，已有 checkpoint 只能作为不完整记录。
- 若必须真正续训，需要先实现并验证 resume 逻辑。
- 若只是重新跑，换新 `run_id`，不要覆盖旧目录。

重新跑示例：

```bash
export run_root_dir=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly
export run_id=awac_critic_pi_state_qwen35_4b_bc30k_onfly_restart1
bash /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/examples/calvin/train_files/run_calvin_awac_critic.sh
```

## 7. 启动 actor

critic 完成后再开 actor。actor 使用同一个 4B BC checkpoint，再加载 critic checkpoint。推荐先确认 `steps_50000_critic.pt` 存在。

```bash
export critic_checkpoint=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly/awac_critic_pi_state_qwen35_4b_bc30k_onfly/checkpoints/steps_50000_critic.pt
test -f "${critic_checkpoint}"

export CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7
export num_processes=8
export WANDB_MODE=offline

export base_vlm=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/playground/Pretrained_models/Qwen3.5-4B
export bc_checkpoint=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260519_h200_server1_pi_state_30k_v1/server1_pi_state/qwen35_4b/pi_state_qwen35_4b_30000step/checkpoints/pi_state_qwen35_4b_30000step/checkpoints/steps_30000_pytorch_model.pt

export calvin_data_root=/inspire/qb-ilm2/project/26summer-camp-10/public/inspire_shared/calvin_abc_d
export data_mix=calvin_abc_d_h200
export include_state=true
export state_dim=8

export compute_rewards_on_the_fly=true
export assume_success_if_missing=false

export run_root_dir=/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly
export run_id=awac_actor_pi_state_qwen35_4b_bc30k_onfly_critic50k

bash /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/examples/calvin/train_files/run_calvin_awac_actor.sh
```

Actor 输出目录：

```text
/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly/awac_actor_pi_state_qwen35_4b_bc30k_onfly_critic50k
```

查看 actor checkpoint：

```bash
find /inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/logs/20260520_awac_pi_state_onfly/awac_actor_pi_state_qwen35_4b_bc30k_onfly_critic50k/checkpoints \
  -maxdepth 1 \
  -type f \
  -name 'steps_*_pytorch_model.pt' \
  -print | sort || true
```

actor 完成后再做 CALVIN eval / rollout 成功率评估。评估命令使用现有文档：

```text
/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/H200_AWAC后训练PIState检查与Rollout命令.md
/inspire/qb-ilm2/project/26summer-camp-10/26220216/starVLA_Project/H200_批量测试命令行.md
```

## 8. 禁止事项

下面这些不要为了跑通而做：

- 不要修改 public parquet。
- 不要对 public 数据跑写入式 `prepare_awac_rewards.py`。
- 不要设置 `assume_success_if_missing=true`；当前数据已有真实 `success`。
- 不要把 9B `steps_25000` 写成 `steps_30000`。
- §4 单源训练不要误开混训；混训 rollout 路径见 §4b，且不要写 public parquet。
- 不要改 `H=8`、`gamma=0.996`、chunk return、last-H done 语义。
- 不要把中断后的重新开始伪装成 resume。
